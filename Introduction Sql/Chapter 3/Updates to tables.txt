# 1. Insert Statement

INSERT INTO instructor VALUES ('10211', 'Smith', 'Biology', 66000);  #Adds a new tuple (row) to the instructor table.

#2. Delete Statement

DELETE FROM student; # Removes all rows from the student table, effectively emptying it.


#3. Drop Table

DROP TABLE r; # Deletes the table r entirely from the database, including all its data and structure.


# 4.  Alter Table

ALTER TABLE r ADD A D; # Adds a new column (A) with the data type (D) to the table r.

# 5. Drop an Attribute

ALTER TABLE r DROP A; # Removes the column (A) from the table 